# RISC-V Cache Coherence Simulator

A simple python simulation of a two core CPU that implements a small subset of the RISC-V instruction set architecture.
This project was the final assesment for a Computer Architecture course. I implemented an MSI protocol for cache coherence of the
multi core CPU.
